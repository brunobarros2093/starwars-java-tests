CREATE TABLE PLANET (id BIGINT PRIMARY KEY AUTO_INCREMENT,
                     name VARCHAR(64) NOT NULL,
                     climate VARCHAR(64) NOT NULL,
                     terrain VARCHAR(64) NOT NULL);
INSERT INTO planet VALUES(1,'tattooine','climate','terrain');
